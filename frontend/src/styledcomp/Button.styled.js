import styled from "@emotion/styled";
import { Button } from "@mui/material";

// For styling Material UI component
const ButtonMui = styled(Button)({
  color: "red",
  backgroundColor: "blue",
  fontSize: "14px",
  marginLeft: "40px",
  borderRadius: "50px",
  padding: "4px 30px",
});

export { ButtonMui };
